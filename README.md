# quiz-dockerized
dockerized version of quiz
## useage
  - install dependencies
  
    ```shell
    yum install -y docker
    yum install -y docker-compose
    ```
    
    
  1. commandline
  
    docker-compose up
    
  2. Background 
  
    docker-compose up -d
